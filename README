# Monthly Weather Forecaster

[![Streamlit App](https://img.shields.io/badge/Streamlit-App-brightgreen)](https://streamlit.io/)
[![Python](https://img.shields.io/badge/Python-3.8%2B-blue)](https://www.python.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

A production-ready **Streamlit web app** for fetching and visualizing monthly weather forecasts (historical or predicted) using the Visual Crossing Weather API. Users can input a city, year, and month to get daily data, summaries, and interactive charts for temperature and precipitation.

**Demo**: [Deployed on Streamlit Cloud](https://your-app-name.streamlit.app) (replace with actual URL after deployment). Defaults to Chennai, November 2025 forecast (warm days ~29°C, heavy rains mid-month ~690 mm total precip).

## Features
- **User-Friendly UI**: Input city (e.g., "Chennai"), year (2020-2030), and month via Streamlit sidebar/forms.
- **Data Fetching**: Pulls daily highs/lows, averages, conditions, and precip from Visual Crossing API (metric units: °C, mm).
- **Visualizations**: 
  - Interactive line charts for temperature trends (high/low/avg).
  - Bar charts for daily precipitation.
- **Summaries**: Metrics for avg high/low temps, total/avg precip.
- **Performance**: Caches API results for 1 hour to respect rate limits (free tier: 1000 requests/day).
- **Error Handling**: Graceful failures for invalid cities/keys/network issues.
- **Production-Ready**: Secure API key management via Streamlit secrets/env vars.

## Prerequisites
- Python 3.8+
- Visual Crossing Weather API key (free: [Sign up here](https://www.visualcrossing.com/weather-api)). 1000 requests/day limit.

## Installation
1. Clone or download the repo:
   ```
   git clone <your-repo-url>
   cd monthly-weather-forecaster
   ```

2. Create a virtual environment (recommended):
   ```
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   ```

3. Install dependencies:
   ```
   pip install -r requirements.txt
   ```

## Usage
1. **Local Development**:
   - Set your API key (dev fallback in sidebar; prod uses secrets/env).
   - Run the app:
     ```
     streamlit run app.py
     ```
   - Opens at `http://localhost:8501`.
   - Enter city/year/month, click "Fetch Monthly Forecast".

2. **Example Inputs**:
   - City: `Chennai` (or `London,UK` for international).
   - Year: `2025`
   - Month: `November`
   - Output: 30-day table, metrics (e.g., Avg High: 29.3°C), and Plotly charts.

3. **Sample Output (Chennai, Nov 2025)**:
   | Date       | Max Temp (°C) | Min Temp (°C) | Precip (mm) | Conditions          |
   |------------|---------------|---------------|-------------|---------------------|
   | 2025-11-01 | 35.0         | 27.0         | 0.1        | Rain, Partially cloudy |
   | ...        | ...          | ...          | ...        | ...                 |
   | 2025-11-30 | 29.3         | 23.0         | 6.7        | Partially cloudy    |

   **Summary Metrics**:
   - Avg High: 29.3°C | Avg Low: 24.4°C | Total Precip: 689.9 mm

## API Key Setup
- **Production (Recommended)**: Use Streamlit secrets or env vars.
  - **Streamlit Cloud**: Create `.streamlit/secrets.toml`:
    ```toml
    WEATHER_API_KEY = "your-key-here"
    ```
    (Don't commit to Git!)
  - **Other Hosts (Heroku/AWS)**: Set env var `WEATHER_API_KEY=your-key-here`.
- **Local Dev**: Falls back to sidebar input if not set.

## Deployment
### Streamlit Cloud (Free & Easy)
1. Push code to GitHub repo (include `app.py`, `requirements.txt`; ignore `secrets.toml`).
2. Go to [share.streamlit.io](https://share.streamlit.io), connect GitHub.
3. Select repo/branch, set app file to `app.py`.
4. In "Secrets", paste from `secrets.toml`.
5. Deploy! Share the public URL.

### Other Options
- **Heroku**: Use `Procfile` with `web: streamlit run app.py --server.port $PORT --server.address 0.0.0.0`.
- **Docker**: Add `Dockerfile`:
  ```
  FROM python:3.9-slim
  WORKDIR /app
  COPY . .
  RUN pip install -r requirements.txt
  EXPOSE 8501
  CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
  ```
- **VPS/AWS**: Run with `nohup streamlit run app.py &`.

## Project Structure
```
monthly-weather-forecaster/
├── app.py              # Main Streamlit app
├── requirements.txt    # Dependencies
├── .streamlit/         # Config folder
│   └── secrets.toml    # API key (gitignored)
└── README.md           # This file
```

## Contributing
1. Fork the repo.
2. Create a feature branch (`git checkout -b feature/amazing-feature`).
3. Commit changes (`git commit -m 'Add amazing feature'`).
4. Push to branch (`git push origin feature/amazing-feature`).
5. Open a Pull Request.

Suggestions: Add ML predictions, more weather params (humidity/wind), or multi-city support.

## License
This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

## Acknowledgments
- [Streamlit](https://streamlit.io/) for the UI framework.
- [Visual Crossing Weather API](https://www.visualcrossing.com/weather-api) for data.

---

*Last Updated: November 12, 2025*  
Questions? Open an issue or reach out! 🚀